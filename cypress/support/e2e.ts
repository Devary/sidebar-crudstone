// Runs before every spec. No custom commands needed yet — sb-sidebar's own controls (the group
// toggle, each link) all carry their own data-cy directly, no PrimeNG p-button wrapper to reach
// through the way search-crudstone's own Filters popover needed.
export {};
